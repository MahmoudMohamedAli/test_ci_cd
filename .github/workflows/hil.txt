name: HIL Test

on:
  workflow_dispatch:

jobs:
  test-runner:
    runs-on: [self-hosted, self-hosted]

    steps:
      - name: Test self-hosted runner
        run: |
          echo "Hello from my HIL machine!"
          echo "Hostname:"
          hostname
          echo "Docker:"
          docker --version